<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Finder</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        }

        .btn-container {
            display: flex;
            gap: 20px;
            margin-top: 30px;
        }

        .btn {
            background: #fff;
            color: #6a11cb;
            font-size: 1.2rem;
            font-weight: bold;
            padding: 10px 25px;
            border: none;
            border-radius: 25px;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn:hover {
            background: #6a11cb;
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.3);
        }

        .footer {
            position: absolute;
            bottom: 20px;
            font-size: 0.9rem;
            text-align: center;
            color: rgba(255, 255, 255, 0.8);
        }

        .footer a {
            color: #fff;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Welcome to Task Finder</h1>
    <p>Organize your tasks and boost your productivity today.</p>
    <div class="btn-container">
        <a href="/register" class="btn">Get Started</a>
        <a href="/login" class="btn">Login</a>
    </div>
    <div class="footer">
        <p>&copy;  Task Finder. All Rights Reserved.</p>
    </div>
</body>
</html>
